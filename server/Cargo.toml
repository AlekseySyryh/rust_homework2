[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
common = { path = "../common" }
ctrlc = "3.5.2"
rand = { version = "0.10.1", features = ["thread_rng"]}
rand_distr = "0.6.0"
clap = { version = "4", features = ["derive"] }
log = "0.4.32"
env_logger = "0.11.10"
